start = "I am "# First, create a variable called start, and set it equal to "I am ".
# Remember the space after the word "am" !


age = 24# Next, create a variable called age and set it equal to your age in years.
# This must be a number


end = " years old"# Next, create a variable called end, and set it equal to " years old".
# Remember the space before the word "years"


output = "{}{}{}".format (start, age, end)# Next, create a variable called output, and use the format() function to add
# together the start, age and end variables. 

# An example output string would be "I am 15 years old"


print (output)# Finally, print the output to the screen using the print() function.
